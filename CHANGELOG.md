# 📋 Таблица версий и история изменений (Build Changelog)

| Билд / Версия | Дата | Статус | Ключевые изменения и исправления |
| :--- | :---: | :---: | :--- |
| **`v7.0.9-custom-b5`** | 08.09.2026 | 🔨 Подготовлен к сборке | • **Зачистка меню Настроек**: из главного экрана настроек полностью вырезаны пункты «Звёзды Telegram» (`main/credits`), «Валюта / TON» (`main/currency`) и кнопка подарков (`main/send-gift`).<br>• **Глубокая блокировка трафика Stories**: в `data_stories.cpp` намертво заблокированы сетевые запросы `MTPstories_GetAllStories`, чтение статусов и фоновая предзагрузка медиа сторис в оперативную память.<br>• **Исправление склейки мульти-поиска (16.png, 17.png)**: в `dialogs_widget.cpp` исправлена очистка результатов при запросах с несколькими словами (`A\|B\|C`). Теперь результаты всех слов объединяются и сортируются по хронологии (`item->date()`).<br>• **Разделение серверных и локальных веток ответов (14.png, 15.png)**: в `data_replies_list.cpp` внедрен метод `isServerThread()`. Для постов каналов включена полноценная серверная пагинация при прокрутке вверх/вниз (`loadBefore`/`loadAfter`).<br>• **Сохранение локального дерева для свободного общения**: для обычных сообщений участников в группах («свободное общение»), не относящихся к постам канала, а также для ЛС сохранена работа клиентского локального дерева ответов. |
| **`v7.0.9-custom-b4`** | 26.08.2026 |  Выпущен | • **Полноценный AST-поиск выражений**: поддержка произвольных выражений со скобками `(замена \| продление) внж -посредники`, точных фраз `"..."`, исключений `-минус` и масок `*`.<br>• **Морфология существительных/глаголов/прилагательных**: исправлен стеммер Портера (декланации `замена/замену/замены/заменой` -> `замен`, `продление/продления/продлению` -> `продлен`), устранены ложные совпадения предлогов (13.png).<br>• **Серверная выгрузка x2 (limit=100)**: размер порции MTProto поиска увеличен до 100 сообщений на запрос с передачей полных слов для серверной токенизации.<br>• **Фикс пустоты справа от вертикальных видео (12.png)**: ширина пузыря сообщений с медиа теперь строго привязана к ширине самого медиа (`media->width()`). |
| **`v7.0.9-custom-b3`** | 25.08.2026 |  Выпущен | • **Раздельные серверные запросы поиска**: отправка каждого ключевого слова отдельным запросом к MTProto API и объединение результатов на клиенте.<br>• **Универсальная клиентская фильтрация**: включен поиск по всем операторам во всех диалогах с дедупликацией по ID.<br>• **Исправление смещения реакций (9.png)**: расчет ширины медиа без текста приведен к естественному размеру, реакциям возвращено корректное позиционирование.<br>• **Ультра-агрессивный ccache**: Direct Mode (`CCACHE_DIRECT=1`) и фиксация компилятора. |
| **`v7.0.9-custom-b2`** | 24.08.2026 |  Выпущен | • **Восстановление комментариев в каналах (7.png/8.png)**: серверные ответы `MTPmessages_GetReplies` для каналов и супергрупп обсуждений.<br>• **Резиновая верстка сообщений**: расширение постов до 85% ширины окна на мониторах высокого разрешения.<br>• **Снижение размера бинарника**: отключены неиспользуемые плагины ввода (`fcitx5`, `hime`, `nimf`) и `kimageformats`, размер снижен с 261 МБ до 251 МБ (в архиве 73.17 МБ). |
| **`v7.0.9-custom-b1`** | 23.08.2026 |  Выпущен | • **Базовый релиз MTProto v7.0.9+**: решена проблема *"сообщение не поддерживается"*.<br>• **Полная зачистка**: удалены RCManager, Sentry, фоновая телеметрия, автоапдейтер, dpaste, iTunes, истории (Stories), подарки, звезды, крипто-интеграция TON. |

---

### 🔍 Подробное описание билда `v7.0.9-custom-b5`

1. **Зачистка настроек от коммерческих элементов**:
   - В `settings_main.cpp` полностью удалены пункты «Звёзды Telegram», «Валюта / TON» и «Подарить Premium».
2. **Отключение сетевых запросов и фоновой предзагрузки Stories**:
   - В `data_stories.cpp` заблокированы методы `loadMore()`, `continuePreloading()`, `startPreloading()`, `incrementPreloadingMainSources()` и `requestReadTills()`, исключая расход трафика и памяти на сторис.
3. **Исправление объединения результатов поиска (16.png, 17.png)**:
   - В `dialogs_widget.cpp` для запросов с `|` результаты всех слов объединяются в общий список без затирания. В `dialogs_inner_widget.cpp` внедрена сортировка по дате.
4. **Серверные треды каналов и пагинация по скроллу (14.png, 15.png)**:
   - В `data_replies_list.cpp` внедрен метод `isServerThread()`: для комментариев каналов включена полноценная серверная пагинация при скролле.
5. **Клиентские локальные ветки для «свободного общения»**:
   - Для обычных сообщений участников в группах, ЛС и базовых чатах сохранено построение локального дерева ответов.

6. **Неограниченное количество локальных папок и облачная синхронизация**:
   - При превышении лимита папок сервера (10 для обычных пользователей / 30 для Premium), все дополнительные папки создаются как локальные (`id >= 1000`) без блокировки интерфейса и без вызова пейволла.
   - Реализована автоматическая синхронизация локальных папок между устройствами пользователя через приватный беззвучный канал-хранилище (`⚙️ AyuGram Sync Storage`) с тегом `#ayugram_sync_folders`.
   - Локальный кэш сохраняется на диск для мгновенного оффлайн-запуска, а при входе с нового устройства папки автоматически загружаются и восстанавливаются из облачного хранилища.
